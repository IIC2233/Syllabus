# habitat_eventualmente_creables — ids de hábitats eventualmente creables
# Ordenados por id_habitat ascendente (orden del generador de entrada desde CSV)

# S — resultado completo: todos los 50 hábitats son eventualmente creables
HABITATS_EVENTUALMENTE_CREABLES_S = list(range(1, 51))

# M — primero, último y largo: todos los 200 hábitats son eventualmente creables
HABITATS_EVENTUALMENTE_CREABLES_M = list(range(1, 201))

# L — primero, último y largo: todos los 700 hábitats son eventualmente creables
HABITATS_EVENTUALMENTE_CREABLES_L = list(range(1, 701))
